# CRON Entry:
# 31 2 * * * /opt/daasi/ldapcleaner/ldapcleaner.pl -c /opt/daasi/cleanRbacSIDs/cleanRbacSIDs.conf

#testmode = 1
loglevel = "info"

ldapuri = "ldap://:389/ou=sessions,ou=rbac,dc=textgrid,dc=de????bindname=cn=rbac,ou=bindaccounts,dc=textgrid,dc=de"
passwordfile = "/opt/daasi/cleanRbacSIDs/localldap.secret"

logfile = "/opt/daasi/cleanRbacSIDs/cleanRbacSIDs.log"
pidfile = "/opt/daasi/cleanRbacSIDs/cleanRbacSIDs.pid"

needoperational = 1

deletefilter = "(&(objectClass=rbacSession)(createTimestamp<%= scope.lookupvar('dhrep::services::tgauth::ldapcleaner_older_than') %>))"
